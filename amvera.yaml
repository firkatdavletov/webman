meta:
  environment: node
  toolchain:
    name: npm
    version: 20

build:
  additionalCommands: npm run build
  artifacts:
    "dist/*": /dist
    "server.js": /
    "package.json": /

run:
  scriptName: server.js
  containerPort: 3000
